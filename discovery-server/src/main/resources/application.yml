server:
  port: 8761

spring:
  application:
    name: discovery-server

# Disable registration for the server itself
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8761/eureka/
